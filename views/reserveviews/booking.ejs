<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container stack">
      <div class="panel">

        <div class="panel-header">
          <h1>Event Reservation</h1>
          <p class="subtle"><%= reservation.EventDetails?.EventName || 'Unknown Event' %> · <%= formatDateTime(reservation.CreatedDateTime) || 'Unknown Date' %></p>
        </div>

        <div class="panel-inner stack">

          <div class="row">
            <span class="pill">Capacity: <%= reservation.EventDetails?.CurrentCapacity || 'Unknown Capacity' %></span>
            <span class="pill">Status: <%= reservation.Status || 'Unknown Booked' %></span>
          </div>

          <% if (reservation.EventDetails?.Booked >= reservation.EventDetails?.Capacity) { %>
              <div class="note note-danger text-danger"><strong>Notice:</strong> This event is full. If you book now, you will be placed on the waitlist.</div>
          <% } %>

          <form id="createReservationForm" class="reserve-form" action="" method="POST">
        
              <label for="numofppl">Number of people attending:</label>
              <input 
                  type="number" 
                  id="numofppl" 
                  class="reserve-input"
                  name="numofppl" 
                  min="1" 
                  step="1"
                  inputmode="numeric"
                  pattern="[0-9]+"
                  max="<%= reservation.EventDetails?.Capacity - reservation.EventDetails?.Booked %>" 
                  value="1"
                  required
              >
              <button class="btn btn-primary btn-sm" type="submit">Confirm</button>
          </form>
    
          <br>

          <a class="btn btn-ghost btn-sm" href="/reserve/reservation">Back</a>
        </div>
      </div>
    </div>
</body>
</html>