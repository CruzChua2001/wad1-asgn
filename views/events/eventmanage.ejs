<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Event Management</title>
</head>

<body>
    <%- include('../partials/navbar') %>

    <h1>Event Management</h1>

    <p>
        <a href="/event/new">Create New Event</a>
    </p>
    
    <!-- TODO: replace this hardcoded table with a dynamic one -->
     <table border="1">
        <thead>
            <tr>
                <th>Event Name</th>
                <th>Event Type</th>
                <th>Current Capacity</th>
                <th>Max Capacity</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody> 
            <tr>
                <td>Event 1</td>
                <td>Type A</td>
                <td>50</td>
                <td>100</td>
                <td>Open</td>
                <!-- TO NOTE: the commented code is the correct way to implement the links -->
                <!-- 
                <td>
                    <a style="color: darkblue; margin-right: 5px;" href="/event/<%#= event.EventID %>">View</a>
                    <a style="text-decoration: underline; color: darkblue;" onClick="deleteEvent(<%#= event.EventID %>)">Delete</a>
                </td>
                 -->
                <td>
                    <a style="color: darkblue; margin-right: 5px;" href="/event/eventid">View</a>
                    <a style="text-decoration: underline; color: darkblue;" onClick="deleteEvent(eventid)">Delete</a>
                </td>
            </tr>
            <tr>
                <td>Event 2</td>
                <td>Type B</td>
                <td>20</td>
                <td>50</td>
                <td>Closed</td>
                <td>
                    <a style="color: darkblue; margin-right: 5px;" href="/event/eventid">View</a>
                    <a style="text-decoration: underline; color: darkblue;" onClick="deleteEvent(eventid)">Delete</a>
                </td>
            </tr>
        </tbody>
     </table>
</body>
</html>

<script>
    const deleteEvent = async (eventid) => {
        if (confirm("Are you sure you want to delete this event?")) {
            // Perform delete operation
            try {
                // TODO: replace this with fetch to delete
                // Example:
                /* 
                const response = await fetch(`/api/events/${eventid}`, {
                    method: 'DELETE'
                });
                */
            } catch (error) {
                console.error("Error deleting event:", error);
                alert("Failed to delete event. Please try again.");
            }
        }
    };
</script>